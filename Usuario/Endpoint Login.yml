info:
  name: Endpoint Login
  type: http
  seq: 4

http:
  method: post
  url: http://localhost:8080/api/v1/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "pedrocoutinho32145@mentoai.com.br",
        "senha": "senhaSegura123"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
