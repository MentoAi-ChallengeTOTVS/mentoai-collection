info:
  name: Listar todas
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/v1/transcricoes"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
