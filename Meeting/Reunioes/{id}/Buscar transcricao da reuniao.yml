info:
  name: Buscar transcricao da reuniao
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/v1/reunioes/:id/transcricao"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
