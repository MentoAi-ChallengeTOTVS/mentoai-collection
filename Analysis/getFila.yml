info:
  name: getFila
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/v1/analises/fila"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
